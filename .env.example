# Environment configuration for BookRec project

# Database credentials:

DB_NAME=db_name # the same for MongoDB and MySQL

# MongoDB specific
MDB_USER=mdb_user
MDB_PASSWORD=mdb_pass
MDB_PORT=mdb_port
MDB_USE_ATLAS=false/true
MDB_CLUSTER=clusterX.*******.mongodb.net
MDB_APPNAME=ClusterX

# MySQL specific
host=host
MSQL_USER=msql_user
MSQL_PASSWORD=msql_pass
MSQL_PORT=msql_port

# Kaggle Api credentials
KAGGLE_USERNAME="username"
KAGGLE_KEY="API_key"

# Possible Environment configuration for BookRec project?

# TODO: Check if we want to keep this here?
# # Minimum activity thresholds
# MIN_USER_RATINGS=5
# MIN_ITEM_RATINGS=5

# # Train/test split and randomness
# TEST_SIZE=0.2
# RANDOM_STATE=42